from distutils.core import setup
setup(
  name = 'affix',
  packages = [''], 
  version = '0.1',
  description = "Seattle's affix-repo package",
  author = 'Seattle',
  url = 'https://github.com/BuildSeash/affix', # use the URL to the github repo
  classifiers = [],
)